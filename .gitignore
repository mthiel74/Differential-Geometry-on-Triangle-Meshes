# Bulk / cached data — regenerable
data/raw/

# Notebook build cache (regenerated on demand)
community/.img-cache/

# Large / regenerable mesh dumps (the small decimated data/trex*.obj test
# meshes ARE committed intentionally; bulk raw scans live in data/raw/)
data/*.ply
data/*.stl
data/*.zip
data/*.tmp

# Python (only if a one-off illustration script is added later)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Wolfram
*.nb~
*.bak
.WolframKernelHistory
$Failed.txt

# We DO intentionally commit final community/*.nb and community/*.pdf

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
